<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Racex</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="mainNav">
      <div class="container">
        <%= link "Racex", to: Routes.page_path(@conn, :index), class: "navbar-brand"  %>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          Menu
          <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
            <%= if @current_user do%>
            <li class="nav-item">
              <a class="nav-link"> <%= @current_user.username %>  </a>
            </li>

            <li class="nav-item">
              <%= link "Create Game", to: Routes.game_path(@conn, :new), class: "nav-link"  %>
            </li>

             <li class="nav-item">
              <%= link "Log out", to: Routes.session_path(@conn, :delete), method: "delete", class: "nav-link"  %>
             </li>
            <% else %>
              <li class="nav-item">
                <a class="nav-link" href="<%= Routes.session_path(@conn, :new)%>"> Login </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="<%= Routes.user_path(@conn, :new)%>"> Register </a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <br>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
